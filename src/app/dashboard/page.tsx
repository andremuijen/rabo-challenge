import { FileUpload } from '@/components/FileUpload/FileUpload';

export default function Page() {
    return (
        <main>
            <h1>Processor</h1>
            <FileUpload />
        </main>
    );
}
